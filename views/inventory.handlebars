
<div class="row">
<div class="col-auto">
    <h2>Welcome, {{user_name}}!</h2>
  </div>
{{#each inventories as |inventory|}}
<div class="Inventory">
 <h2>
   {{product_id}}
    {{entry_price}}
    {{owned_stocks}}
    {{inventory_valuation}}
    </h2>
{{/each}}
</div>