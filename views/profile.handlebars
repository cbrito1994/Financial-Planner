<div>
  {{logged_in}}
<h1> Welcome, {{{user_name}}}!</h1>
</div>
<div>
    <h1>Here's the information about your stocks!</h1>
    {{#each inventories as |inventory|}}
<div class="inventory">
  <h2>
    {{{entry_price}}}
  </h2>
  <h2>
    {{{owned_stocks}}}
  </h2>
  <h2>
    {{{inventory_valuation}}}
  </h2>
</div>
{{/each}}
</div>